kind = "web"
previewPath = "/"
title = "PRD to Sprint Planner"
version = "1.0.0"
id = "artifacts/prd-sprint-planner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19333

[services.development]
run = "pnpm --filter @workspace/prd-sprint-planner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/prd-sprint-planner", "run", "build" ]
publicDir = "artifacts/prd-sprint-planner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19333"
BASE_PATH = "/"
